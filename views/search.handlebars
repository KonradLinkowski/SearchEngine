{{#each webPages}}
  <article>
    <a href={{this.url}}><h2>{{this.title}}</h2></a>
    <a href={{this.url}}>{{this.url}}</a>
    <p>{{this.description}}</p>
  </article>
{{/each}}

{{#each pages}}
<a>{{this}}</a>

{{/each}}
